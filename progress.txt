# BrowserStrike — Notes for next iteration

## Completed this session
- TASK-012 (Colyseus State Schema)
  - Created packages/server/src/schemas/ with 4 schema files + barrel export:
    - GameState.ts: roomCode, status (GameStatus), settings (SettingsSchema), adminId, scoreTeamA/B, currentRound, roundTimer, players (MapSchema<PlayerSchema>), killFeed (ArraySchema<KillEventSchema>)
    - PlayerSchema.ts: sessionId, nickname, team, isAlive, hp, position (x/y/z float32), rotation (yaw/pitch float32), currentWeapon, ammo, isReloading, kills, deaths
    - SettingsSchema.ts: mode (GameMode), mapId, roundsToWin, roundTimeLimit
    - KillEventSchema.ts: killerNickname, victimNickname, weapon, isHeadshot, timestamp
  - All schemas use @type() decorators for Colyseus serialization
  - Added experimentalDecorators: true to server tsconfig.json
  - Updated GameRoom to use GameState: setState(), generates room code, creates PlayerSchema on join, assigns admin, handles leave/admin reassignment

## Current state
- TASK-001 through TASK-008 + TASK-012 all done
- Server boots on port 2567 with full state schema, room code generation, player management
- Client builds and type-checks; 3D warehouse scene with full FPS controls, gravity, jump, AABB collisions
- Desert Eagle model visible in lower-right, LMB fires with recoil animation

## Next priority tasks (all deps met)
- TASK-013: Room system — create/join by code, nickname validation, max 4 players (deps: TASK-012 ✓) — critical, blocks lobby/UI chain
- TASK-009: Shooting system / raycasting (deps: TASK-008 ✓) — high, muzzle flash + bullet tracer + impact marks

## Notes
- @colyseus/schema v2.0.37 uses legacy decorators (experimentalDecorators: true)
- Room code uses ABCDEFGHJKLMNPQRSTUVWXYZ23456789 charset (no 0/O/1/I confusion)
- PlayerSchema uses flat float32 for position/rotation (not nested Schema) for bandwidth efficiency
- GameState.players is MapSchema<PlayerSchema> keyed by sessionId
- GameState.killFeed is ArraySchema<KillEventSchema>
- PLAYER_SPEED = 250 u/s, GRAVITY = -20, JUMP_VELOCITY = 8, EYE_HEIGHT = 1.65m
- PLAYER_RADIUS = 0.3, PLAYER_HEIGHT = 1.8 (collision capsule approximated as AABB)
- applyMovement() is a pure function — returns new state, doesn't mutate input. Ready for server use.
- CollisionWorld.resolve() runs up to 4 iterations to handle corner sliding
- Mouse sensitivity = 0.002, Euler order YXZ for proper FPS camera
- Colyseus 0.15.57 on server, colyseus.js ^0.15.0 on client
- Scene uses PCFSoftShadowMap, fog (30-60 range), directional light shadow camera covers 25 units
- Weapon overlay uses separate scene+camera with autoClear=false, clearDepth before render
- Deagle recoil: 120ms duration, sin-based kick curve, 0.04 up / 0.06 back / 0.15 rad rotation
